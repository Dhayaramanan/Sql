CREATE TABLE parent (children TINYINT);
-- tinyint, bigint, mediumint, int, smallint have various range of integers
-- that we can store

CREATE TABLE someone (jogging_km TINYINT UNSIGNED); -- only +ve values
